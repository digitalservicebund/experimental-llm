---
all:
  hosts:
    llm-server-01:
      ansible_host: REPLACE_WITH_PUBLIC_IP
      ansible_user: ubuntu
      ansible_ssh_private_key_file: ~/.ssh/REPLACE_WITH_KEY_NAME
  children:
    llm_servers:
      hosts:
        llm-server-01:
