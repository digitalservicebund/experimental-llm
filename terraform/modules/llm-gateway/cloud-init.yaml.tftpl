#cloud-config
hostname: ${hostname}
manage_etc_hosts: true
ssh_pwauth: false
users:
  - name: ${ssh_user}
    groups: [adm, sudo]
    shell: /bin/bash
    lock_passwd: true
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh_authorized_keys:
      - ${bootstrap_ssh_public_key}
